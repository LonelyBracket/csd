import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
})

export const metadata: Metadata = {
  title: 'Control+Shift+Deliver - Tech Leaders Who Deliver',
  description: 'A B2B podcast featuring tech leaders, engineers, and innovators sharing insights on building great software and teams. Topics include DevOps, Platform Engineering, SRE, and technical leadership.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className={inter.variable}>
      <body className={inter.className}>{children}</body>
    </html>
  )
}
